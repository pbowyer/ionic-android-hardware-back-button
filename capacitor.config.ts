import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-android-hardware-back-button',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
